let left_operand = 5
let right_operand = 5

let operator ="+";
let answers;

if(operator == "+") {
console.log(left_operand + right_operand);
}

else if(operator == "*") {
console.log(left_operand * right_operand);
}